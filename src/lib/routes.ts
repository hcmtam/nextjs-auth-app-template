export const AUTH_ROUTES = ["/sign/login", "/sign/sign-up"];
export const PUBLIC_ROUTES = [...AUTH_ROUTES];

export const apiAuthPrefix = "/api/auth";

export const LOGIN_PAGE_REDIRECT = "/sign/login";
export const LOGIN_SUCCESS_REDIRECT = "/u/dashboard";
